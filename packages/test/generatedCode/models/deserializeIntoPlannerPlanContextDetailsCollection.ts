import {PlannerPlanContextDetailsCollection} from './index';
import {AdditionalDataHolder, Parsable, ParseNode, SerializationWriter} from '@microsoft/kiota-abstractions';

export function deserializeIntoPlannerPlanContextDetailsCollection(plannerPlanContextDetailsCollection: PlannerPlanContextDetailsCollection | undefined = {}) : Record<string, (node: ParseNode) => void> {
    return {
    }
}
