import {PlannerPlanContextCollection} from './index';
import {AdditionalDataHolder, Parsable, ParseNode, SerializationWriter} from '@microsoft/kiota-abstractions';

export function serializePlannerPlanContextCollection(writer: SerializationWriter, plannerPlanContextCollection: PlannerPlanContextCollection | undefined = {}) : void {
}
