/** Provides operations to manage the collection of device entities. */
export enum WebsiteType {
    Other = "other",
    Home = "home",
    Work = "work",
    Blog = "blog",
    Profile = "profile",
}
