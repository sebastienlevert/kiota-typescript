import {PlannerFavoritePlanReferenceCollection} from './index';
import {AdditionalDataHolder, Parsable, ParseNode, SerializationWriter} from '@microsoft/kiota-abstractions';

export function deserializeIntoPlannerFavoritePlanReferenceCollection(plannerFavoritePlanReferenceCollection: PlannerFavoritePlanReferenceCollection | undefined = {}) : Record<string, (node: ParseNode) => void> {
    return {
    }
}
